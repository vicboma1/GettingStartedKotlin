dist: trusty
language: java
sudo: false
notifications:
  email:
    on_success: never
    on_failure: change
cache:
  directories:
       - $HOME/.m2

# use operating systems.
os:
  - linux
  
jdk:
  - openjdk11
  
script:
  - mvn test -f ./pom.xml
